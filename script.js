'use strict'

function filterRangeInPlace(arr, a, b) {
  arr.map(() => {
    if (a > item || b < item)
  });
}

let arr = [5, 3, 8, 1];

filterRangeInPlace(arr, 1, 4); // удалены числа вне диапазона 1..4

alert( arr ); // [3, 1]


 